// Side-effect import: registers the self-hosted Geist + Geist Mono variable fonts
// (woff2) used only by the landing. Imported once from the landing entry so the
// marketing type never falls back to a default stack. No exports.
import '@fontsource-variable/geist'
import '@fontsource-variable/geist-mono'
