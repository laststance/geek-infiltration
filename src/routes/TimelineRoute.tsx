import TimelineContainer from '@/app/TimelineContainer'

/**
 * Renders the existing authenticated timeline view at its URL route.
 * @returns The current timeline container.
 * @example
 * <Route path="/app" Component={Component} />
 */
export function Component() {
  return <TimelineContainer />
}
